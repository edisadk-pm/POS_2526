package graphics;

import utils.Vector;

public class Moveable {
    protected Vector location; // x, y
    protected Vector velocity; // current velocity
    protected Vector acceleration; // acceleration
}